# Assumptions

- The workspace-specified stack (Next.js + FastAPI) is the desired initial target.
- The project will start single-user and self-hosted for MVP.
- Ollama may be present on developer machines but is not guaranteed; connectors must handle absence.
- No initial constraint on API provider costs — cost-awareness will be added in Stage 2.
